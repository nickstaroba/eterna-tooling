# Eterna Lint-staged Config for ESLint

## Installation

```bash
npm i --save-dev @eterna/lint-staged-config-eslint
```

## Usage

Example `.lintstagedrc.js`

```javascript
const config = require("@eterna/lint-staged-config-eslint");

module.exports = {
  ...config,
  ...
}
```
