# Eterna Jest Config for TypeScript React Projects

## Installation

```bash
npm i --save-dev @eterna/jest-config-typescript-react
```

## Usage

Example `jest.config.js`

```javascript
const config = require("@eterna/jest-config-typescript-react");

module.exports = {
  ...config,
  ...
}
```
